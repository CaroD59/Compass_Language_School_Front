import styled from 'styled-components';

const StyledHeader = styled.div`
  .NavBar {
    .LogoContainer {
      a {
        img {
          width: 10vw;
          padding-left: 10px;
        }
      }
    }
  }
`;

export default StyledHeader;
